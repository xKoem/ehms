//
// Created by Koem on 13/12/2017.
//

#include "Person.h"

string Person::getId() {
    return id;
}

string Person::getName() {
    return name;
}

string Person::getSurname() {
    return surname;
}

string Person::getAccountNumber() {
    return accountNumber;
}

//Person::Person(string id, string name, string surname, string acc): id(id), name(name), surname(surname), accountNumber(accountNumber) {}
